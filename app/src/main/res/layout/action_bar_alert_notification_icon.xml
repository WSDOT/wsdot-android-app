<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:layout_gravity="center"
    android:clickable="true"
    style="@android:style/Widget.ActionButton">

    <ImageView
        android:id="@+id/main_icon"
        android:src="@drawable/ic_menu_info"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:layout_margin="0dp"
        android:contentDescription="best times to travel"
        />

    <TextView xmlns:android="http://schemas.android.com/apk/res/android"
        android:id="@+id/menu_alert"
        android:layout_width="wrap_content"
        android:minWidth="17sp"
        android:textSize="12sp"
        android:textColor="#ffffffff"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="!"
        android:layout_alignTop="@id/main_icon"
        android:layout_alignRight="@id/main_icon"
        android:layout_alignEnd="@+id/main_icon"
        android:layout_marginRight="-4dp"
        android:layout_marginEnd="-4dp"
        android:layout_marginTop="-6dp"
        android:paddingBottom="1dp"
        android:paddingRight="1dp"
        android:paddingLeft="1dp"
        android:background="@drawable/square_badge"/>
</RelativeLayout>